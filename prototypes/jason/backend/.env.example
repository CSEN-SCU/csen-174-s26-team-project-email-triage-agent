ANTHROPIC_API_KEY=sk-ant-...
TRIAGE_MODEL=claude-sonnet-4-6
CLASSIFY_MODEL=claude-haiku-4-5-20251001
CORS_ORIGINS=http://localhost:3000
# Optional: when set, inbox data is read from Postgres via asyncpg (see docker-compose.yml).
# postgresql://… is rewritten to postgresql+asyncpg:// automatically.
# DATABASE_URL=postgresql://triage:triage@localhost:5432/triage
