# Copy to `backend/.env` for local runs (see README in this folder).

DATABASE_URL=sqlite:///./ethan_triage.db
CORS_ORIGINS=http://localhost:3000

# Required for POST /api/emails/{id}/analyze (OpenAI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
