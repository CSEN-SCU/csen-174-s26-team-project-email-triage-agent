# Copy to `.env` and fill in values. See prototypes/ethan/frontend/README.md for run steps.

# Next.js / NextAuth (required for the dashboard in prototypes/ethan/frontend)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# Optional: LLM provider for future triage / summarization features (uncomment what you use)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
